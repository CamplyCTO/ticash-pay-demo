# Copy to .env and adjust. Used by the API and the migration runner.
DATABASE_URL=postgres://ticash:ticash@localhost:5432/ticash
PG_POOL_MAX=10
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=info

# Set STORE=memory to run the API against the in-memory store (no Postgres needed).
# STORE=memory
